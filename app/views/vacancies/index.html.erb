<% if @vacancies.empty? %>
  <div class="warning">Nenhuma vaga cadastrada</div>
<% end %>
<%= link_to 'Cadastrar uma Vaga', new_vacancy_path %>
<% @vacancies.each do |vacancy| %>
<dl>
  <dt>Título</dt>
  <dd><%= link_to vacancy.title, vacancy %></dd>
  <dt>Descrição</dt>
  <dd><%= vacancy.description %></dd>
</dl>
<% end %>